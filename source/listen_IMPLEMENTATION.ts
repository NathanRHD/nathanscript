// import { getListen } from './core/listen';

// export const listen = getListen<ActionCreators>()

/**
 * An example of actually using the listen implementation in the client would be useful! That being said, there probably
 * isn't much of a use for it *usually* outside of core.
 */